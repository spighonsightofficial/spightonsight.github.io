import Text from './text.override';
import Heading from './heading.override';
import FormLabel from './form-label.override';
import FormErrorMessage from './form-error-message.override';

const components = {
  Text,
  Heading,
  FormLabel,
  FormErrorMessage,
};

export default components;
